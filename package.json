{
  "name": "blue-pi-test",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"pnpm -F api dev\" \"pnpm -F client dev\"",
    "dev:api": "pnpm -F api dev",
    "dev:client": "pnpm -F client dev",
    "start": "concurrently \"pnpm -F api start\" \"pnpm -F client start\"",
    "deploy:api": "pnpm -F api deploy ./api/.deploy",
    "deploy:client": "pnpm -F client deploy ./client/.deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
